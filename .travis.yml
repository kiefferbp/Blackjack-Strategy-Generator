language: java
sudo: true
before_install:
- chmod +x gradlew
matrix:
  include:
    - jdk: oraclejdk8
      env: TEST="test.HardHandTest"
    - jdk: oraclejdk8
      env: TEST="test.SoftHandTest"
script: ./gradlew test -i --tests $TEST