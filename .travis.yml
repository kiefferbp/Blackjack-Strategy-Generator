language: java
sudo: true
jdk:
- oraclejdk8
before_install:
- chmod +x gradlew
matrix:
  include:
    - env: TEST="test.HardHandTest"
    - env: TEST="test.SoftHandTest"
script: ./gradlew test -i --tests $TEST